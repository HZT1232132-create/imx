# i.MX93 仓储标签异常鲁棒识别与分拣告警模拟系统 — 实现详解

## 一、这个项目是干什么的？

想象一个大型仓库，传送带上的包裹需要自动识别并送到正确的区域（A区、B区、C区）。每个包裹上贴有标签，包含 QR 二维码和印刷文字。

本系统模拟的就是这样一个**智能分拣终端**的大脑，运行在 i.MX93 嵌入式芯片上。它的工作流程：

```
包裹到达 → 读标签 → 识别包裹ID → 查规则表 → 判断分拣是否正确 → 触发告警
```

核心难点在于：**真实仓库中标签经常损坏**——二维码可能磨损、印刷文字可能模糊。所以系统设计了多级容错机制。

---

## 二、系统的三大运行模式

| 模式 | QR失败后做什么 | 纠错能力 |
|------|---------------|---------|
| baseline | 直接报错 | 无 |
| ocr | 用OCR识别文字，仅精确匹配 | 弱 |
| full | OCR + 编辑距离纠错 | 强 |

---

## 三、整体架构（9 阶段流水线）

main.cpp 的 `main()` 函数就是一个流水线，每个包裹依次经过 9 个阶段：

```
Stage 1: 加载规则表 (rules.csv)
Stage 2: 加载测试序列 (demo_sequence.csv)
Stage 3: 初始化识别器 (QR + OCR)
Stage 4: 初始化纠错器 + 风险引擎 + 告警器 + 日志器
          ↓
    对每一帧画面，依次执行：
          ↓
Stage 1: QR 码识别 (PrimaryRecognizer)
Stage 2: OCR 文字识别 (OCRRecognizer，仅QR失败时)
Stage 3: OCR 结果纠错 (OCRCorrector)
Stage 4: 分拣判断 (RuleEngine)
Stage 5: 风险评估 (RiskEngine)
Stage 6: 处理计时
Stage 7: GPIO 告警模拟 (AlarmSimulator)
Stage 8: 日志与统计 (EventLogger + StatsManager)
Stage 9: 画面渲染 (OpenCV HUD)
```

---

## 四、数据文件

### 4.1 rules.csv — 规则表

```csv
package_id,name,target_zone,type
PKG001,电子元件,A,normal
PKG002,机械零件,B,normal
PKG003,食品包装,A,normal
PKG004,医疗用品,C,fragile
PKG005,测试货物,B,test
```

每行定义一个包裹：ID、名称、应去的目标区域、类型。

### 4.2 demo_sequence.csv — 测试序列

```csv
image,current_zone,scene
pkg001_normal.png,A,正常分拣
pkg002_wrong.png,A,错误异常
pkg003_qr_damaged.png,A,二维码损坏OCR补救
pkg004_ocr_error.png,A,OCR字符识别纠错处理
pkg005_unknown.png,A,未知货物
pkg006_unreadable.png,A,标签不可读
```

定义了 6 个测试场景，每帧包含图片名、当前区域、场景描述。

---

## 五、核心模块详解

### 5.1 InputSource — 输入源

**文件**：`src/input_source.cpp`

**做了什么**：
- 读取 `demo_sequence.csv`
- 把每一行解析成 `InputFrame` 结构体（图片路径、当前区域、场景描述）
- 提供 `hasNext()` / `next()` 接口，像播放列表一样逐个取用

**关键代码**（input_source.cpp:27-59）：
```cpp
// 读取CSV，跳过表头，逐行解析
std::getline(file, line);  // 跳表头
while (std::getline(file, line)) {
    std::stringstream ss(line);
    std::getline(ss, image, ',');   // 图片文件名
    std::getline(ss, zone, ',');    // 当前区域
    std::getline(ss, scene, ',');   // 场景描述
    // 拼接完整路径
    frame.imagePath = m_imageDir + "/" + image;
}
```

### 5.2 PrimaryRecognizer — QR 码识别

**文件**：`src/recognizer.cpp`（前半部分）

**做了什么**：
- 用 OpenCV 内置的 `QRCodeDetector` 扫描图片
- 如果扫到 QR 码，提取其中的包裹 ID
- 提取逻辑：从 QR 码文本中匹配 `PKG` 开头的字符串，优先选纯数字后缀（如 PKG003 优于 PKGO03）

**关键代码**（recognizer.cpp:54-84）：
```cpp
std::string qrData = m_qrDetector.detectAndDecode(image);
if (!qrData.empty()) {
    result.packageId = extractPackageId(qrData);  // 提取PKGxxx
    result.success = true;
    result.method = "QR";
}
```

**extractPackageId 的聪明之处**：
- 用正则表达式 `PKG[0-9OIL]{3,}` 找到所有候选
- 优先选纯数字后缀的（避免 OCR 混淆字符）
- 因为 QR 码本身不会出错，但 QR 码内容可能包含多余文字

### 5.3 OCRRecognizer — OCR 文字识别

**文件**：`src/recognizer.cpp`（后半部分）

**做了什么**：
- 仅在 QR 码识别失败时调用
- 使用 Tesseract OCR 引擎读取图片中的文字
- 限制识别字符集为 `A-Z` 和 `0-9`（包裹ID只可能是这些字符）
- 多区域扫描（Multi-ROI）：在 5 个不同区域分别尝试，哪个先找到 PKG 就用哪个

**为什么需要多区域扫描？**
因为标签上的文字可能出现在不同位置——可能在上方的标题栏、下方的文字区、右侧的 QR 码旁边等等。

**5 个 ROI 区域**（recognizer.cpp:124-152）：
```
ROI 1: 全图
ROI 2: 底部 15%（标签文字通常在这里）
ROI 3: 顶部 12%（ID / 标题行）
ROI 4: 右半部分
ROI 5: 右下角象限
```

**图像预处理**（recognizer.cpp:186-206）：
```cpp
// 转灰度 → 放大（如果太小）→ 自适应二值化
cv::cvtColor(image, gray, cv::COLOR_BGR2GRAY);
cv::resize(gray, scaled, cv::Size(), scale, scale, cv::INTER_CUBIC);
cv::adaptiveThreshold(scaled, binary, 255, ADAPTIVE_THRESH_GAUSSIAN_C, THRESH_BINARY, 15, 8);
```

### 5.4 OCRCorrector — OCR 纠错器（核心创新）

**文件**：`src/ocr_corrector.cpp`

**为什么需要纠错？**
OCR 引擎经常把字符认错，比如：
- 字母 `O` 被识别成数字 `0`
- 字母 `I` 被识别成数字 `1`
- 字符粘连导致丢字或多字

例如：真实 ID 是 `PKG001`，但 OCR 读成了 `PKGOOI`（两个字母O和一个字母I）

**纠错三步走**（corrector.cpp:11-67）：

```
输入: "PKGOOI"
  │
  ├─ 第1步：清洗（去除非字母数字字符，转大写）
  │  → cleaned = "PKGOOI"
  │  → 在规则表中精确查找 → 没找到
  │
  ├─ 第2步：归一化混淆字符（O→0, I→1, L→1），再精确匹配
  │  → normalized = "PKG001"
  │  → 在规则表中精确查找 → 找到了！→ OCR_CORRECTED
  │
  └─ 第3步（第2步失败才走）：编辑距离搜索
     → 计算 normalized 与规则表中每个ID的编辑距离
     → 找距离最小的，距离 ≤ 阈值（短ID=1, 长ID=2）就算匹配
```

**编辑距离（Levenshtein Distance）**（corrector.cpp:90-105）：
衡量两个字符串的差异程度。允许的操作：替换、插入、删除一个字符。

```
例: "KITTEN" → "SITTING" = 3步
    1. K→S (替换)
    2. E→I (替换)  
    3. 插入G
```

实现用经典的动态规划算法（二维 DP 表）。

### 5.5 RuleEngine — 规则引擎

**文件**：`src/rule_engine.cpp`

**做了什么**：
- 加载 `rules.csv`，建立 `packageId → PackageRule` 的映射表
- 提供三个查询接口：
  - `hasPackage(id)` — 这个包裹ID在规则表中吗？
  - `getTargetZone(id)` — 这个包裹应该去哪个区？
  - `getRules()` — 返回全部规则（给纠错器用）

**关键数据结构**（ocr_corrector.h:14-19）：
```cpp
struct PackageRule {
    std::string packageId;   // 如 "PKG001"
    std::string name;        // 如 "电子元件"
    std::string targetZone;  // 如 "A"
    std::string type;        // "normal" / "fragile" / "test"
};
```

### 5.6 RiskEngine — 风险引擎

**文件**：`src/risk_engine.cpp`

**风险等级定义**（risk_engine.h:19-25）：

| 等级 | 含义 | 触发条件 |
|------|------|---------|
| LEVEL_0 | 正常 | QR成功 + 正确分拣 |
| LEVEL_1 | 低风险 | OCR恢复成功（QR坏了但OCR修正了） |
| LEVEL_2 | 中风险 | OCR纠错成功（OCR读错但算法修正了） |
| LEVEL_3 | 高风险 | 未知货物 / 标签完全不可读 |
| LEVEL_4 | 危急 | 分拣错误（货物去了错误区域！） |

**映射逻辑**（risk_engine.cpp:3-26）：
```cpp
// 判断优先级：分拣错误 > 标签异常 > OCR修正 > OCR恢复 > 正常
if (sortStatus == WRONG_SORT)     → LEVEL_4  // 最严重！
if (idStatus == LABEL_ERROR)      → LEVEL_3
if (idStatus == UNKNOWN_PACKAGE)  → LEVEL_3
if (idStatus == OCR_CORRECTED)    → LEVEL_2
if (idStatus == OCR_RECOVERED)    → LEVEL_1
otherwise                          → LEVEL_0
```

### 5.7 AlarmSimulator — 告警模拟器

**文件**：`src/alarm_simulator.cpp`

**模拟 GPIO 引脚控制**：

| 风险等级 | 绿灯 | 黄灯 | 红灯 | 蜂鸣器 |
|---------|------|------|------|--------|
| LEVEL_0 | ON | OFF | OFF | OFF |
| LEVEL_1 | ON | ON | OFF | OFF |
| LEVEL_2 | OFF | ON | OFF | OFF |
| LEVEL_3 | OFF | OFF | ON | OFF |
| LEVEL_4 | OFF | OFF | ON | **ON** |

在真实的 i.MX93 芯片上，这些就是 GPIO 引脚的高低电平信号，控制实际的 LED 灯和蜂鸣器。

### 5.8 EventLogger — 事件日志器

**文件**：`src/logger.cpp`

**做了什么**：
- 把每一帧的处理结果写入 CSV 文件
- CSV 转义处理（包含逗号/引号的字段用双引号包裹）
- 带时间戳

**输出格式**：
```csv
time,image,scene,raw_text,final_package_id,recognition_method,id_status,...
```

### 5.9 StatsManager — 统计管理器

**文件**：`src/stats.cpp`

**做了什么**：
- 实时统计各类事件数量
- 计算成功率指标：
  - 综合识别成功率 = (QR成功 + OCR恢复 + OCR纠正) / 总数
  - OCR 恢复率 = OCR成功数 / OCR尝试数
  - OCR 纠正率 = 纠错成功数 / OCR总成功数
- 维护最近 5 条高风险事件（在 HUD 上滚动显示）

### 5.10 drawHUD — 可视化渲染

**文件**：`main.cpp:18-87`

**做了什么**：
- 在图片上叠加半透明黑色蒙版
- 顶部显示：帧号、图片名、场景描述、包裹ID、识别方法
- 中部显示：目标区域、当前区域、分拣状态
- 风险等级用颜色区分（绿/黄/红）
- 底部状态栏 + 最近高风险事件滚动

---

## 六、完整处理流程（以帧4为例）

帧 4 是最复杂的场景，走完了所有模块：

```
图片: pkg004_ocr_error.png
真实情况: 无QR码，印刷文字为 "PKG00I" (故意把1印成I)
当前区域: A区
目标区域: PKG001 应去 A区

Step 1 - QR识别:
  detectAndDecode() → 空的（没有QR码）
  → QR FAILED

Step 2 - OCR识别:
  多ROI扫描 → 读到文字 "PKGOOI"
  extractPackageId() → "PKGOOI"
  → OCR SUCCESS, raw="PKGOOI"

Step 3 - OCR纠错:
  cleanText("PKGOOI") → "PKGOOI" (已经是大写)
  精确匹配 "PKGOOI" → 规则表没有 → 失败
  normalizeOcrConfusions("PKGOOI") → "PKG001" (O→0, I→1)
  精确匹配 "PKG001" → 规则表有！→ 成功
  → OCR_CORRECTED, finalPackageId="PKG001"

Step 4 - 分拣判断:
  getTargetZone("PKG001") → "A"
  targetZone("A") == currentZone("A") → 一致
  → NORMAL_SORT

Step 5 - 风险评估:
  OCR_CORRECTED + NORMAL_SORT
  → LEVEL_2_MEDIUM（需要人工复核）

Step 6 - 告警:
  LEVEL_2 → 黄灯亮，其余灭

Step 7 - 日志:
  写入 events.csv

Step 8 - 显示:
  HUD 显示所有信息，黄灯警告
```

---

## 七、6个测试场景总结

| 帧 | 图片 | 关键挑战 | 识别路径 | 风险 |
|----|------|---------|---------|------|
| 1 | pkg001_normal | 无（正常） | QR → PKG001 | LEVEL_0 绿 |
| 2 | pkg002_wrong | 分拣错误 | QR → PKG002 | **LEVEL_4 红+蜂鸣** |
| 3 | pkg003_qr_damaged | QR码物理损坏 | QR失败→OCR→PKG003 | LEVEL_1 绿+黄 |
| 4 | pkg004_ocr_error | OCR字符混淆(PKG00I) | QR失败→OCR→纠错→PKG001 | LEVEL_2 黄 |
| 5 | pkg005_unknown | 包裹不在规则库 | QR→PKG999→无匹配 | LEVEL_3 红 |
| 6 | pkg006_unreadable | 标签完全不可读 | QR失败→OCR失败 | LEVEL_3 红 |

---

## 八、目录结构一览

```
imx93_sorting_sim/
├── main.cpp              ← 主程序，9阶段流水线 + HUD渲染
├── include/              ← 8个头文件（类声明 + 枚举定义）
│   ├── process_result.h     结果结构体
│   ├── rule_engine.h        规则引擎 + PackageRule
│   ├── risk_engine.h        IdStatus / SortStatus / RiskLevel 枚举
│   ├── recognizer.h         QR + OCR 识别器接口
│   ├── ocr_corrector.h      纠错器 + CorrectionResult
│   ├── input_source.h       输入源
│   ├── alarm_simulator.h    告警模拟器
│   ├── stats.h              统计管理器
│   └── logger.h             日志器
├── src/                  ← 8个实现文件
│   ├── input_source.cpp     CSV读取，帧序列管理
│   ├── recognizer.cpp       QR检测 + Tesseract OCR + 多ROI
│   ├── ocr_corrector.cpp    文本清洗 + 混淆归一化 + 编辑距离
│   ├── rule_engine.cpp      CSV加载 + 规则查询
│   ├── risk_engine.cpp      5级风险映射
│   ├── alarm_simulator.cpp  GPIO模拟输出
│   ├── logger.cpp           CSV日志写入
│   └── stats.cpp            统计计算 + 汇总打印
├── data/
│   ├── rules.csv            规则表（5条包裹规则）
│   ├── demo_sequence.csv    测试序列（6个场景）
│   ├── expected.csv         预期结果
│   └── images/              6张测试图片
├── generate_images.py      用 qrcode + PIL 生成测试图片
├── validate.py              验证日志与预期是否一致
├── CMakeLists.txt           CMake构建配置
├── build/                   编译输出目录
├── output/                  运行后保存的带HUD帧画面
└── logs/                    运行日志
```

---

## 九、如何自己编译运行

### 安装依赖
```bash
sudo apt install cmake g++ make pkg-config \
  libopencv-dev libtesseract-dev libleptonica-dev \
  tesseract-ocr-eng
```

### 编译
```bash
cd build
cmake ..
make -j$(nproc)
```

### 运行
```bash
# 交互模式（按任意键切帧，Q退出）
./sorting_sim

# 自动播放模式（每帧1秒）
./sorting_sim ../data/demo_sequence.csv ../data/rules.csv ../data/images 1000
```

### 参数说明
```
./sorting_sim [序列CSV] [规则CSV] [图片目录] [等待毫秒] [模式]
                                                      baseline/ocr/full
```

---

## 十、关键技术点总结

1. **多级容错**：QR → OCR → 编辑距离纠错，三级递进
2. **混淆字符归一化**：利用领域知识（包裹ID格式为PKG+数字），将 OCR 常见混淆（O→0, I→1）预先修正
3. **多 ROI 扫描**：在图像不同区域分别 OCR，提高文字定位成功率
4. **自适应阈值二值化**：改善不同光照条件下的 OCR 效果
5. **风险分级告警**：不只判断对错，而是给出 5 级风险，不同级别触发不同 GPIO 信号
